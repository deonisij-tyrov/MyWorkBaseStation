1.Скрипты для БД находятся в ./resources/dataBaseScript:
2.В таблице юзеров создан один юзер как 'admin', остальные добавляются как 'user'
(login - denis; password-denis);
3.url - http://localhost:8080/stations

-В проекте реализовал добавление, изменение параметров и удаление сот для станций в базе данных.
-Реализована регистрация, логирование пользователей. Пользователь с ролью 'admin' имеет возможность менять статус юзера.
-Создан фильтыр для обработки исключений. В зависимости от типа исключения пользователь получает информацию по возможной причине.
-Numberformatexception не пробрасывается в методе 'service' класса 'FrontController extends HttpServlet'-
поэтому я пробрасываю именно 'RuntimeExeptions' для возможности обработки фильтром.
-Bootstrap локально не использую(поздно вспомнил) поэтому запусткать лучше с наличием выхода в сеть:)

При спорной оценке готов досдать задачи если будет возможность. Все лежит на github.